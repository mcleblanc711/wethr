# Core
httpx>=0.27.0              # Async HTTP client
numpy>=1.26.0              # Ensemble math
scipy>=1.12.0              # EMOS/NGR calibration (optimize, stats)
pyarrow>=16.0.0             # Immutable Parquet calibration archives

# Polymarket
py-clob-client>=0.15.0     # Official CLOB SDK (live trading)

# Data — sqlite3 is stdlib, no extra dep

# Optional: calibration visualization
# matplotlib>=3.8.0
# seaborn>=0.13.0
